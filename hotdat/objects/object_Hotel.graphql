# Hotel type
type Hotel implements Node {
    # Hotel ID
    code: ID!

    # Hotel data
    hotelData: HotelData

  # ID code
  code: ID!
  # Errors that will lead the service to abort
  error: [Error!]
  # Create date 
  createdAt: DateTime!
  # Update date
  updatedAt: DateTime!
}
