type Access implements Node{
  code: ID!

  accessData: AccessData

  # Errors that will lead the service to abort
  error: [Error!]

  # Create date
  createdAt: DateTime!

  # Update date
  updatedAt: DateTime!
}
