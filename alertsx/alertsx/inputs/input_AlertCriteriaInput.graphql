# Filters needed to do a search.
input AlertCriteriaInput {
  "Alert codes"
  codes: [ID!]
  #active or not
  isActive: Boolean
  "Group codes"
  groups: [ID!]
  #List of shared type  
  shared: [SharedType!]
}
