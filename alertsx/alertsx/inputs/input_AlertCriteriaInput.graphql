# Filters needed to do a search.
input AlertCriteriaInput {
  # List of alerts
  alertCodes: [ID!]
  #active or not
  isActive: Boolean
  #List of group codes where alerts are located
  groups: [ID!]
}
