type Product implements Node{
  # ID code
  code: ID!
  # Organization Data
  productData: ProductData!
  
  # Errors that will lead the service to abort
  error: [Error!]
  # Create date 
  createdAt: DateTime!
  # Update date
  updatedAt: DateTime!
}
