# Hotel Type
type Hotel implements Node {
  # Hotel ID
  code: ID!
  
  # Hotel data
  hotelData: HotelData
  
  # Errors that abort services
  error: [AdviseMessage!]
  
  # Date created
  createdAt: DateTime!
  
  # Date updated
  updatedAt: DateTime!
}
