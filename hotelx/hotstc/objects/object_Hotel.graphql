# Hotel Type
type Hotel implements Node {
  # Hotel ID
  code: ID!
  
  # Hotel data
  hotelData: HotelData
  
  # Errors that abort services
  error: [Error!]
  
  # Date created
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
