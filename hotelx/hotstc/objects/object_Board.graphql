# Board Type
type Board implements Node {
  # Board ID
  code: ID!
  
  # Board data
  boardData: BoardData
  
  # Errors that abort services
  error: [Error!]
  
  # Create date
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
