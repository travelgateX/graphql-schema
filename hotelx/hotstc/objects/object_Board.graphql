# Board Type
type Board implements Node {
  # Board ID
  code: ID!
  
  # Board data
  boardData: BoardData
  
  # Errors that abort services
  error: [Error!]
  
  # Date created
  createdAt: DateTime!
  
  # Date updated
  updatedAt: DateTime!
}
