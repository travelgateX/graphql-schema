# Category Type
type Category implements Node {
  # Category ID
  code: ID!
  
  # Category data
  categoryData: CategoryData
  
  # Errors that abort services
  error: [Error!]
  
  # Date created
  createdAt: DateTime!
  
  # Date updated
  updatedAt: DateTime!
}
