type Generic{
    # Informs if the supplier requires nationality in Search, Quote and Book.
    requiredNationality: ReviewedBool

    # If true, the supplier informs the onrequest status option in Search, Quote and Book.
    allowsOnRequest: ReviewedBool

    # Informs if the supplier allows nationality in Search, Quote and Book.
    allowsNationality: ReviewedBool
}