# Hotel Destination data
type HotelDestinationData {
  # destination code
  code: ID!

  # name of destination
  naem: String!

  # Parent destination
  parent: HotelDestination

  # Indicates destination type, zone, city or country
  type: DestinationType

  # Geographical coordinates corresponding to a location.
  coordinates: Coordinates

}
