# Airport Type
type Airport implements Node{
    # Airport ID
    code: ID!

    # Airport data
    airportData: AirportData

    # Errors that abort services
    error: [Error!]

    # Date created
    createdAt: DateTime!

    # Date updated
    updatedAt: DateTime!

}