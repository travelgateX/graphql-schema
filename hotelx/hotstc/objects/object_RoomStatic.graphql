# Room Type
type RoomStatic implements Node {
  # Room ID
  code: ID!
  
  # Room data
  roomData: RoomData
  
  # Errors that abort services
  error: [Error!]
  
  # Create date
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
