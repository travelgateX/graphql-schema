# Input BookPax contains basic information about pax such as name, surname and age.
input BookPaxInput {
  # Passenger first  name
  name: String!
  
  # Passenger  last name
  surname: String!
  
  # Passenger age on the day of check-in
  age: Int!

  # Passenger title.
  title: PersonalTitleType
}
