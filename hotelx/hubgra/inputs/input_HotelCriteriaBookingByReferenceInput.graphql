# Booking query criteria, which contains basic information in order to find a created book or books with our TravelgateX unique locator.
input HotelCriteriaBookingByReferenceInput {
  # Name of the access that have to use in the request.
  accessCode: String!
  
  # The language code.
  language: Language
  
  # TravelgateX booking locators.
  references: [String!]!
}
