type Validation {
  # Indicates the current status of the validation
  status: ValidationStatusType!
  # Indicates the scenario being validated
  scenario: String!
  # Indicates the errors that caused the validation to fail
  error: [String!]
}