type TrafficOptimizationHotelBlacklistConnection {
  totalCount: Int!
  pageInfo: PageInfo!
  edges: [TrafficOptimizationHotelBlacklistEdge]
  adviseMessage(level: [AdviseMessageLevel]): [AdviseMessage!]
}
