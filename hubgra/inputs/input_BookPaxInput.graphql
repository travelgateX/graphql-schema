# Input BookPax contains basic information abaout pax like name, surname and age.
input BookPaxInput {
    # Guest's name
	name: String!

	# Guest's last name
	surname: String!

	# Guest's age
	age: Int!
}
