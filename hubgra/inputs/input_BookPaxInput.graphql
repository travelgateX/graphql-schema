# Input BookPax contains basic information abaout pax such as name, surname and age.
input BookPaxInput {
    # The guest's first  name
	  name: String!

	  # The guest's last name
	  surname: String!

    # The guest's age
	  age: Int!
}
