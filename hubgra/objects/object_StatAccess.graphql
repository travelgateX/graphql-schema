type StatAccess {
  name:                   String!
  total:                  Stat!
  staticConfiguration:    Stat
  hotels:                 Int!
  zones:                  Int!
  cities:                 Int!
  dockerID:               String!
  rqAccess:               StatPlugin
  rsAccess:               StatPlugin
  transactions:           [StatTransaction!]!
  plugins:               [StatPlugin!]
}
