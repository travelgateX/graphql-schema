# Plugin Step Type
enum PluginStepType {
    # Aggregate Access Response.
    AGGREGATE_ACCESS

    # Aggregate Response.
    AGGREGATE
  
    # Room code and descriptions mapping
    MAPPING_ROOM

    # Markup application
    MARKUP
}
