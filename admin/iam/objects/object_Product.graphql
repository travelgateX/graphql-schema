# An APIs collection.
type Product implements Node{
  code: ID!
  productData: ProductData!
  # Errors that abort services
  error: [Error!]
  # Date created
  createdAt: DateTime!
  # Date updated
  updatedAt: DateTime!
}
