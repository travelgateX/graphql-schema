type Member implements Node{
  code: ID!
  memberData: MemberData
  # Errors that abort services
  isActive: Boolean!
  error:   [Error!]
  # Date created
  createdAt: DateTime!
  # Date updated
  updatedAt: DateTime!
}
