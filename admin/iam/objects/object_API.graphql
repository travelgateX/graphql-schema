type API implements Node{
  code: ID!
  apiData: APIData
  # List of advise messages.
  adviseMessage(level: [AdviseMessageLevel]): [AdviseMessage!]
  # Date created
  createdAt: DateTime!
  # Date updated
  updatedAt: DateTime!
}
