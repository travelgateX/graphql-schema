input UpdateProfileInput {
  # Profile Code
  code: ID!
  # Label of the Profile
  label: String
  # Profile type
  type: ProfileType
  # Active profile
  isActive: Boolean
  # Profile is published
  isPublished: Boolean 
}
