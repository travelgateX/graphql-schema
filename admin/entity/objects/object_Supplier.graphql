type Supplier implements Node{
  code: ID!
  supplierData: SupplierData
  # Errors that abort services
  error: [Error!]
  # Create date
  createdAt: DateTime!
  # Update date
  updatedAt: DateTime!
}
