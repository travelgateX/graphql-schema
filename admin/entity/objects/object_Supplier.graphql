type Supplier implements Node{
  code: ID!
  supplierData: SupplierData
  
  # Errors that will lead the service to abort
  error: [Error!]
  
  # Create date
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
