# Context type, for now only Code is necessary
type Context implements Node {
  code: ID!
  
  # Errors that abort services
  error: [Error!]
  
  # Create date
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
