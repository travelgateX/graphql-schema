type HubProvider implements Node {
  code: ID!
  hubProviderData: HubProviderData
  # Errors that will lead the service to abort
  adviseMessage(level: [AdviseMessageLevel]): [AdviseMessage!]
  # Create date
  createdAt: DateTime!
  # Update date
  updatedAt: DateTime!
}
