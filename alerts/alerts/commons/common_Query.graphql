# The query type, represents all of the entry points into our object graph
extend type Query {     #extended
  #The alertsX product query root for implementing GraphQL query.
  alertsX: AlertsXQuery
}
