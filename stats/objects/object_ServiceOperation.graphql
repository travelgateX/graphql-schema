# Specific method of API used.
type ServiceOperation implements Node{
  # Service operation code.
  code: ID!
  
  serviceOperationData:ServiceOperationData!
  
  # Errors that will lead the service to abort
  error: [Error!]
  
  # Create date
  createdAt: DateTime!
  
  # Update date
  updatedAt: DateTime!
}
