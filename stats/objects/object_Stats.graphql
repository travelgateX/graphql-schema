# The service used to access the stats of every connection that uses the HUB
type Stats {
	# Search stats
	search: TravelOperation!

	# Quote stats
	quote: TravelOperation!

	# Booking stats
	booking: TravelOperation!
}
