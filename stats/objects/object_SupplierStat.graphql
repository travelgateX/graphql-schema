# Destination of request sent by HUB
type SupplierStat {
  
  # Total hits.
  hits: Int!
  # OKs responses
  okHits: Int!
  #Total time
  time: Int!
  errors(type: StatsErrorType!, code:StatsCodeError): [StatsError!]
  # Details of look to quote and look to book.
  lookTo: LookTo!
}
