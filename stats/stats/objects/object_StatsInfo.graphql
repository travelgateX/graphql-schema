# Details of an specific error
type StatsInfo {
  code: ID!
  # Stats type
  type: StatsInfoTypes!
  # Total hits
  hits: Int64!
  # Average time (ms)
  time: Int!
  # Details about errors.
  assets:[StatsAsset!]
}
