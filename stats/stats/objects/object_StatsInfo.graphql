# Details of an specific error
type StatsInfo {
  code: ID!
  # Stats type
  type: StatsInfoTypes!
  # Total hits
  hits: Int!
  # Average time (ms)
  time: Int!
  # Details about errors.
  asserts:[StatsAssert!]
}
